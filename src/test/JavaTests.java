package controlenov;

import org.junit.jupiter.api.Test;

public class JavaTests {
	
	bool x = true;
	
	@Test
	 void dumbTest() {
		assertFalse(x);
	}
	
	/*
	 * vrai test :
	 * 
	 * @test
	 * void affectionTest(){
	 * g.affecter(s);
	 * assertTrue(g.sujetAffecté() == s || s.groupe() == g)
	 *  
	 * 
	 * }
	 */
}
